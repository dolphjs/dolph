Prioritized refactor plan — status (May 2026)

[x] Fix config parsing correctness in core/dolph_factory.core.ts
    Port from YAML coerced with Number() + Number.isFinite + > 0.

[x] Stabilize TypeScript type environment
    skipLibCheck in tsconfig.json; contributor notes in docs/architecture.md (TypeScript section).

[x] Replace deprecated sample APIs (MediaParser) with current uploader APIs
    Samples use useFileUploader / fileUploader; architecture doc calls this out.

[x] Unify controller DI contract
    ComponentParams typings relaxed; canonical patterns documented in docs/architecture.md (DI section).

[x] Harden and expand integration tests for decorator argument injection
    tests/decorator_args.test.ts (incl. DQuery), jwt_auth_parity, multipart_upload_route, socket_smoke, uploader tests.

[x] Add CI workflow for test + type-check + lint
    .github/workflows/ci.yml

[x] Document canonical architecture + sample conventions
    docs/architecture.md + main README; samples/mega-rest-api/README.md includes cURL catalog.

[x] Audit middleware stubs/no-op code paths
    DolphAsyncMiddleware, DolphAsyncMiddlewareDec, DolphMiddleware execute and return Promises where applicable; notes in architecture.md.

Future (optional)
- Consider eslint --fix across the whole repo in CI when noise is acceptable.
- Long-term: optional GraphQL stack alignment if @dolphjs/graphql peer graph changes again.
