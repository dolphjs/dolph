database:
  mongo:
    url: sensitive
    options:
  # mysql:
  #   host: localhost
  #   database: dolph
  #   user: root
  #   pass:
middlewares:
  cors:
    activate: true
    origin: ''
    methods:
      - GET
      - POST
      - PUT
      - DELETE
    allowedHeaders:
routing:
  base: '/v1'
port: 3030
jsonLimit: 20mb
# env: production
